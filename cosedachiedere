1. prevedere rotta di registrazione utenti?
2. il login e controllo del db della validità della pass si può fare nel middleware? middleware è un componente del controller
3. casi d'uso si fanno con include? 
4. rotta per sapere il numero di token?
5. MVP o mvc
6. i pattern che abbiamo usato vanno bene?
7. cosa intendeva per la generazione dei JWT su jwt.io
