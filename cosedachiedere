1. prevedere rotta di registrazione utenti? no, solo login
2. il login e controllo del db della validità della pass si può fare nel middleware? middleware è un componente del controller (no, il middleware fa solo controlli base)
3. casi d'uso si fanno con include? si, mettere validation
4. rotta per sapere il numero di token? si da fare
5. MVP o mvc   mc(mvc)
6. i pattern che abbiamo usato vanno bene? Si
7. cosa intendeva per la generazione dei JWT su jwt.io, ok




1. l'admin ha pure lui la possibilità di giocare e avere token? l'admin si può ricaricare i token da solo?
2. ogni richiesta tranne /playerrank deve avere token>0?
3. i token per la creazione della partita vanno tolti ad entrambi gli utenti?
4. I token per le mosse vanno tolti 0.1 ad ogni utente che esegue una mossa
6. Creare una rotta per verificare le partite svolte, deve essere per utente? o di tutti?
7. Creare una rotta per valutare lo stato di una data partita, deve essere una partita dell'utente autenticato o anche di altri utenti?
8. l'esame è in presenza?